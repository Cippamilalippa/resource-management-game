# mods/

Third-party mods live here, each in its own folder with the **same shape as
`/content`** (a `manifest.json`, `prototypes/`, optional `scripts/`). They are
loaded through the exact same pipeline as the base game — there is no privileged
path for first-party content.

Empty for now.
